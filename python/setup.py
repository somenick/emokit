from setuptools import setup, find_packages

setup(
    name = "emotiv",
    version = "0.1",
    packages = find_packages(),
    install_requires = [],
    author = "Daeken",
    description = "Python library for the Emotiv headset",
    url = "https://github.com/daeken/Emokit"
)
